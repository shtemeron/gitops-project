# AWS's Gateway API implementation doesn't support the standard Gateway
# API certificateRefs field for TLS — the cert ARN goes here instead, via
# this AWS-specific CRD, referenced from the Gateway's
# spec.infrastructure.parametersRef. Confirmed against
# kubernetes-sigs/aws-load-balancer-controller's own docs, not assumed.
apiVersion: gateway.k8s.aws/v1
kind: LoadBalancerConfiguration
metadata:
  name: prod-gateway-lbconfig
  namespace: prod
spec:
  # Unspecified, this is "automatically inferred" per AWS's own docs —
  # which empirically landed on "internal" here. Explicit, not left to
  # inference: this needs to be reachable from outside the VPC entirely
  # (Stage 6's definition of done), the same as dev's Ingress already
  # gets via its own explicit "internet-facing" annotation.
  scheme: internet-facing
  listenerConfigurations:
    - protocolPort: HTTPS:443
      defaultCertificate: arn:aws:acm:us-east-1:100282333708:certificate/9a47a3a4-751f-4e15-81f0-4c6b228a1435
  defaultTargetGroupConfiguration:
    name: prod-gateway-defaults