serviceAccountName: dev-workload

app:
  image:
    tag: manual-db21dea
  hpa:
    minReplicas: 1
    maxReplicas: 3

postgres:
  storageClassName: gp3-dev
  internalHostname: postgres-dev.gitops-project.internal
